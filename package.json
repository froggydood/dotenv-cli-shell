{
  "name": "dotenv-cli-shell",
  "version": "0.0.1",
  "description": "Import environment variables from files into shell",
  "bin": {
    "dotenv": "./cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "dotenv": "^16.0.3",
    "dotenv-expand": "^10.0.0",
    "minimist": "^1.2.8"
  }
}
